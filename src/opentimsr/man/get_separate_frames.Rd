% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/opentimsr.R
\name{get_separate_frames}
\alias{get_separate_frames}
\title{Get frames as separate data frames.}
\usage{
get_separate_frames(opentims, frames, columns = all_columns)
}
\arguments{
\item{opentims}{Instance of OpenTIMS.}

\item{frames}{Vector of frame numbers to extract.}

\item{columns}{Vector of columns to extract. Defaults to all columns.}
}
\value{
A named list of data.frames, one per distinct frame, in order of
first request and named by frame number. Each data.frame has the requested
columns, with one row per peak, as returned by \code{\link{query}}.
}
\description{
Like \code{\link{query}}, but returns one data frame per frame instead of
one table with the peaks of all frames. Frames are decoded in parallel
(see \code{\link{opentims_set_threads}}). A frame requested more than once
is returned once.
}
\examples{
path.d = system.file("extdata", "test.d", package = "opentimsr")
setup_opensource()
D = OpenTIMS(path.d)
frames = get_separate_frames(D, c(1, 2))
names(frames)
head(frames[["2"]])
get_separate_frames(D, 1, columns=c('scan','intensity')) # only 'scan' and 'intensity'
CloseTIMS(D)
}
